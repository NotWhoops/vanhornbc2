 // Brandon Van Horn

 // Date Revised: 3/3/26 

 // All For One NextJS Rebuild

 // recreated the all for one api build with flowbite react and tailwind in nextjs with components for each mini challenge and the fetches are done in a js file

// Peer Review: Isaias Gonzalez - home page crashes for what seems like two different reasons. terminal says "name is not defined" `(app\page.tsx:98:7)`, while the browser says "result is not defined" `app/page.tsx (130:27)`.
